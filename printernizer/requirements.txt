# Printernizer - Professional 3D Print Management System
# Requirements for Milestone 1.2: Printer API Integration

# Core Framework
fastapi==0.128.0
uvicorn[standard]==0.40.0
pydantic>=2.9.0
pydantic-settings>=2.6.0

# WebSocket Support for Real-time Updates
websockets==12.0

# Database - Using aiosqlite for async SQLite
aiosqlite==0.19.0

# HTTP Client for Prusa API
aiohttp>=3.10.0
requests>=2.32.4
Brotli>=1.1.0  # Brotli compression support for aiohttp

# MQTT Client for Bambu Lab Integration
paho-mqtt>=2.0.0
bambulabs-api>=1.0.0  # Primary library for Bambu Lab file operations

# Network Discovery
zeroconf>=0.132.0  # mDNS/Bonjour discovery for Prusa printers
ssdpy>=0.4.1  # SSDP protocol for Bambu Lab printer discovery
netifaces-plus>=0.12.0  # Network interface enumeration (Windows-compatible fork of netifaces)

# File Download and Management
aiofiles==23.2.1
watchdog==3.0.0

# Structured Logging
structlog==23.2.0

# Prometheus Metrics
prometheus-client==0.19.0

# Configuration Management
python-dotenv==1.2.2

# Date and Time Handling
python-dateutil==2.8.2

# German Business Support
pytz==2023.3

# Performance Monitoring
psutil==5.9.6

# Test dependencies live in requirements-test.txt

# Production Server
gunicorn>=22.0.0

# File Processing and Validation
# magic==0.4.27
filetype==1.2.0
openpyxl>=3.1.0  # Excel file generation for material exports

# 3D File Processing and Preview Rendering
trimesh>=4.0.5
numpy-stl>=3.0.1
scipy>=1.11.0
networkx>=3.0  # Required by trimesh for 3MF mesh processing
# NOTE: build123d (parametric model generator) is an OPTIONAL dependency for the
# same reason as matplotlib: cadquery-ocp (OpenCascade bindings) ships glibc
# manylinux wheels for x86_64/aarch64 only — no musllinux build and no 32-bit
# armv7 wheel. Listing it here would break the Alpine and armv7 image builds, so
# it lives in requirements-optional.txt and is installed best-effort per platform
# (see docker/Dockerfile and the HA add-on Dockerfile). The generator degrades
# gracefully when build123d is not importable.
# NOTE: matplotlib is an OPTIONAL dependency (STL/3MF preview thumbnail
# rendering only - see src/services/preview_render_service.py, which degrades
# gracefully when it is missing). It is intentionally NOT listed here because
# it has no musllinux/aarch64 wheel and cannot be compiled inside the
# Alpine-based Home Assistant add-on image. Install requirements-optional.txt
# on glibc platforms to enable STL preview rendering.

# Security Headers and Middleware
python-multipart==0.0.27
# cryptography>=42.0.0  # Temporarily disabled for Python 3.13 compatibility

# URL Parsing and Web Content Extraction (for Ideas feature)
beautifulsoup4==4.12.2

# Image Processing for Thumbnails (Ideas feature)
Pillow>=10.0.0

# Documentation - MkDocs Material for GitHub Pages
mkdocs-material>=9.5.0
mkdocstrings[python]>=0.24.0  # Auto-generate Python API docs from docstrings
mkdocs-awesome-pages-plugin>=2.9.0  # Better page organization
mkdocs-git-revision-date-localized-plugin>=1.2.0  # Show last update dates
pymdown-extensions>=10.7.0  # Enhanced markdown features
