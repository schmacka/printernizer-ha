"""Bundled build123d model-generator templates.

Each template is a module exposing a ``build(**params)`` function that returns a
build123d shape, paired with a ``<name>.json`` sidecar describing its parameters.
"""
